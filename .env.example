# Google Meet MCP Server Environment Configuration Example
# Copy this file to .env and configure with your own paths

# Primary configuration (recommended)
# The token will be automatically saved alongside your credentials file
G_OAUTH_CREDENTIALS="/path/to/your/credentials.json"

# Alternative configuration (for advanced users)
# GOOGLE_MEET_CREDENTIALS_PATH="credentials.json" 
# GOOGLE_MEET_TOKEN_PATH="token.json"

# Optional: Node.js configuration
# NODE_ENV=production
# NODE_OPTIONS="--max-old-space-size=512"

# Optional: Logging configuration
# LOG_LEVEL=info
# LOG_FILE="logs/mcp-server.log"

# Security Note:
# - Never commit your actual .env file with real paths
# - Keep your credentials.json file secure and local
# - Use absolute paths for better reliability