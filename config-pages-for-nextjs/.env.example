# Google Meet MCP Integration - Environment Variables

# MongoDB Connection (required)
MONGODB_URI="mongodb://localhost:27017/your-database"
# Or for MongoDB Atlas:
# MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/database"

# NextAuth Configuration (required if using NextAuth)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# MCP Encryption Key (recommended - if not set, will use NEXTAUTH_SECRET)
ENCRYPTION_KEY="your-32-character-encryption-key-for-aes256-encryption-here"

# Optional: Node.js Configuration
NODE_ENV="development"
# NODE_OPTIONS="--max-old-space-size=512"

# Optional: Additional Security
# ADMIN_API_KEY="admin-key-for-management-endpoints"

# Notes:
# 1. ENCRYPTION_KEY should be 32+ characters for AES-256
# 2. If ENCRYPTION_KEY is not set, NEXTAUTH_SECRET will be used
# 3. Keep this file secure and never commit real values to git
# 4. For production, use your platform's secret management system