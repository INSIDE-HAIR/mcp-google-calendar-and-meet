# Local Environment Configuration (NOT COMMITTED TO GIT)
# This file contains your actual local paths

# Primary configuration (recommended for v3.0)
# Option 1: Direct Token Authentication
CLIENT_ID=your-client-id.apps.googleusercontent.com
CLIENT_SECRET=GOCSPX-your-client-secret
REFRESH_TOKEN=1//your-refresh-token

# Option 2: File-based Authentication (legacy)
G_OAUTH_CREDENTIALS="/absolute/path/to/your/credentials.json"

# Token will be automatically created alongside credentials file

# ========== COMANDO DE SETUP (COPY & PASTE) ==========
# Cuando expire el token, ejecuta este comando en terminal:
# G_OAUTH_CREDENTIALS="/path/to/your/credentials.json" npm run setup

# Optional: Node.js configuration  
NODE_ENV=production
NODE_OPTIONS="--max-old-space-size=512"

# Optional: Monitoring and debugging
ENABLE_HEALTH_CHECK=true
HEALTH_CHECK_PORT=9090
LOG_LEVEL=info

# Optional: Calendar defaults
GOOGLE_CALENDAR_DEFAULT_TIMEZONE=Europe/Madrid
GOOGLE_CALENDAR_DEFAULT_CALENDAR_ID=your-email@domain.com